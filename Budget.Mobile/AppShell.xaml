<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Budget.Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Budget.Mobile"
    xmlns:views="clr-namespace:Budget.Mobile.Views"
    Title="Budget.Mobile">

    <TabBar>
        <ShellContent Title="Dashboard" ContentTemplate="{DataTemplate views:DashboardPage}" />
        <ShellContent Title="Budgets" ContentTemplate="{DataTemplate views:BudgetPage}" />
        <ShellContent Title="Ajouter" ContentTemplate="{DataTemplate views:AddTransactionPage}" />
        <ShellContent Title="Calendrier" ContentTemplate="{DataTemplate views:CalendarPage}" />
        <ShellContent Title="Historique" ContentTemplate="{DataTemplate views:TransactionsPage}" />
    </TabBar>
</Shell>
